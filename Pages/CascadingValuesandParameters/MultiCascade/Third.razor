<h3>Third</h3>
<div>
    Transferred Value = @(TransferredValue.Name + " " + TransferredValue.Age)
</div>
@code {
    

    //Vì có nhiều value khác nhau nên nếu muốn chọn đúng value phải truyền tên vào
    //Ở đây lấy value có Name là Cascade3 
    [CascadingParameter(Name = "Cascade3")]
    public MultiCascading.Person TransferredValue { get; set; }
}
