@using Microsoft.AspNetCore.Components.Web @* contain blazor built-in components *@
@using Microsoft.AspNetCore.Components.Routing @* is used for rendering and mapping routes to components *@
@* it means urls mapped to components *@


@using FirstBlazorApp.Shared @* imports the contents of Shared folder *@
@* these contents are navigations,stylesheets, layouts and you name it  *@
@* that are frequently shared among different razor components *@

@* You can also specify layouts in the Blazor Imports file for razor component. 
For example 
@layout MyLayout - ensures that all of the components use MyLayout. *@